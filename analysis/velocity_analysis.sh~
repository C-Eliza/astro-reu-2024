#!/bin/bash

# Generate simulations with different pixel sizes
impix=(50 100 150 200 300 400 500)
for impix in 50 100 150 200 300 400 500; do
    python HIIsim.py --impix $impix --fnamebase "test_${impix}"
done
